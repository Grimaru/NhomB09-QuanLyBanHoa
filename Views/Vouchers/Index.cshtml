@model IEnumerable<demo2.Models.Voucher>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CodeVoucher)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PhantramVoucher)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HansudungVoucher)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tinhtrang)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CodeVoucher)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhantramVoucher)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HansudungVoucher)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tinhtrang)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.IDVoucher }) |
                @Html.ActionLink("Details", "Details", new { id=item.IDVoucher }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.IDVoucher })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
